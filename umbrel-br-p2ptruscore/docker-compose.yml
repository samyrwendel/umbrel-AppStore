version: '3'

services:
  app_proxy:
    environment:
      APP_HOST: app
      APP_PORT: 3000

  app:
    image: node:20-alpine
    restart: unless-stopped
    command: 
      - sh
      - -c
      - |
        echo "🚀 P2P TrustScore Bot v4.1.0 - FORÇA ATUALIZAÇÃO..."
        apk add --no-cache git
        if [ ! -d /app/.git ]; then
          echo "📥 Clonando repositório..."
          git clone https://github.com/samyrwendel/p2ptruscore.git /app
        else
          echo "🔄 Atualizando código..."
          cd /app && git pull origin main
        fi
        cd /app
        echo "📦 Instalando todas as dependências..."
        npm install
        echo "🏗️ Tentando compilar com npx (sem instalação global)..."
        timeout 60 npx @nestjs/cli build || echo "⚠️ Build demorou muito ou falhou..."
        echo "🔍 Verificando se build foi criado..."
        if [ -f "/app/dist/main.js" ]; then
          echo "✅ Build criado com sucesso!"
          echo "🌐 Iniciando servidor com interface de configuração..."
          echo "✅ Acesse /api/config para configurar o bot"
          npm run start:prod
        else
          echo "❌ Build falhou, iniciando servidor HTTP simples..."
          node -e "
          const http = require('http');
          const server = http.createServer((req, res) => {
            res.writeHead(200, {'Content-Type': 'text/html; charset=utf-8'});
            if (req.url === '/api/config') {
              res.end('<html><head><title>Configuração Bot</title></head><body><h1>🤖 P2P TrustScore Bot</h1><p>⚠️ Interface de configuração temporariamente indisponível</p><p>O bot está em modo de manutenção devido a problemas de compilação.</p></body></html>');
            } else {
              res.end('<html><head><title>P2P TrustScore Bot</title></head><body><h1>🤖 P2P TrustScore Bot</h1><p>✅ Servidor funcionando!</p><p>⚠️ Bot em modo de manutenção</p><p><a href=\"/api/config\">Ir para configuração</a></p></body></html>');
            }
          });
          server.listen(3000, () => {
            console.log('✅ Servidor HTTP simples rodando na porta 3000');
            console.log('🌐 Acesse via botão Abrir no Umbrel');
          });
          "
        fi
    volumes:
      - ${APP_DATA_DIR}/data:/data
    environment:
      NODE_ENV: production
