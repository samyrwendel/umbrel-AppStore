version: '3'

services:
  app_proxy:
    environment:
      APP_HOST: app
      APP_PORT: 3000

  app:
    image: node:20-alpine
    restart: unless-stopped
    command: 
      - sh
      - -c
      - |
        echo "🚀 P2P TrustScore Bot v4.0.2 - CORREÇÃO TIPOS..."
        apk add --no-cache git
        if [ ! -d /app/.git ]; then
          echo "📥 Clonando repositório..."
          git clone https://github.com/samyrwendel/p2ptruscore.git /app
        else
          echo "🔄 Atualizando código..."
          cd /app && git pull origin main
        fi
        cd /app
        echo "📦 Instalando dependências de produção..."
        npm install --only=production
        echo "📦 Instalando dependências de desenvolvimento..."
        npm install --only=development
        echo "🔧 Instalando tipos Node.js..."
        npm install @types/node --save-dev
        echo "🔧 Instalando cross-env..."
        npm install cross-env --save-dev
        echo "🔧 Instalando NestJS CLI globalmente..."
        npm install -g @nestjs/cli
        echo "🏗️ Compilando projeto (ignorando erros de tipo)..."
        if command -v nest >/dev/null 2>&1; then
          nest build --skip-type-check || echo "⚠️ Build com avisos, mas continuando..."
        else
          echo "⚠️ Usando npx como fallback..."
          npx @nestjs/cli build --skip-type-check || echo "⚠️ Build com avisos, mas continuando..."
        fi
        echo "🔍 Verificando se build foi criado..."
        if [ -f "/app/dist/main.js" ]; then
          echo "✅ Build criado com sucesso!"
          echo "🌐 Iniciando servidor com interface de configuração..."
          echo "✅ Acesse /api/config para configurar o bot"
          npm run start:prod
        else
          echo "❌ Build falhou, tentando compilar com tsc diretamente..."
          npx tsc --skipLibCheck --outDir dist src/main.ts || echo "⚠️ Compilação com avisos..."
          if [ -f "/app/dist/main.js" ]; then
            echo "✅ Compilação manual funcionou!"
            node dist/main.js
          else
            echo "❌ Todas as tentativas falharam, iniciando servidor simples..."
            node -e "
            const express = require('express');
            const app = express();
            app.get('/', (req, res) => res.send('Bot em manutenção - Problemas de compilação'));
            app.get('/api/config', (req, res) => res.send('Interface de configuração temporariamente indisponível'));
            app.listen(3000, () => console.log('Servidor temporário rodando na porta 3000'));
            " || echo "❌ Falha total"
          fi
        fi
    volumes:
      - ${APP_DATA_DIR}/data:/data
    environment:
      NODE_ENV: production
